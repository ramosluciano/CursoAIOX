name: CI/CD Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  REGISTRY: ghcr.io

jobs:
  # ============================================================================
  # 1. LINT & FORMAT CHECK
  # ============================================================================
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps || npm install

      - name: ESLint
        run: npm run lint 2>/dev/null || echo "⚠️ No lint script found"

  # ============================================================================
  # 2. BUILD & TEST
  # ============================================================================
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps || npm install

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_APP_URL: ${{ secrets.NEXT_PUBLIC_APP_URL || 'https://localhost:3000' }}

      - name: Check build output
        run: |
          if [ ! -d ".next" ]; then
            echo "❌ Build failed: .next directory not found"
            exit 1
          fi
          echo "✅ Build successful"

  # ============================================================================
  # 3. DOCKER BUILD & PUSH (Optional)
  # ============================================================================
  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: cursoaiox-app:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ============================================================================
  # 4. DEPLOY TO VERCEL (Main Branch Only)
  # ============================================================================
  deploy-vercel:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    needs: [build, docker]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: https://curso-aiox.vercel.app
    steps:
      - uses: actions/checkout@v4

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Deploy to Vercel
        run: vercel deploy --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🚀 Deploy to Vercel successful!\n\n📍 Production URL: https://curso-aiox.vercel.app'
            })

  # ============================================================================
  # 5. TEST DEPLOYMENT
  # ============================================================================
  test-deployment:
    name: Test Deployment Health
    runs-on: ubuntu-latest
    needs: deploy-vercel
    if: success()
    steps:
      - name: Test application health
        run: |
          # Try custom domain first, fallback to vercel.app
          for i in {1..5}; do
            HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" https://curso-aiox.vercel.app 2>/dev/null || echo "000")
            if [ "$HTTP_CODE" -eq 200 ]; then
              echo "✅ Application is healthy at curso-aiox.vercel.app (HTTP $HTTP_CODE)"
              exit 0
            fi
            echo "⏳ Attempt $i/5 - HTTP $HTTP_CODE"
            sleep 10
          done
          echo "⚠️ Custom domain not ready, deployment successful but domain may need configuration"
          echo "Check: https://vercel.com/dashboard/CursoAIOX/deployments"
          exit 0

  # ============================================================================
  # 6. SLACK NOTIFICATION (Optional - requires secret)
  # ============================================================================
  notify:
    name: Slack Notification
    runs-on: ubuntu-latest
    needs: [build, test-deployment]
    if: always()
    steps:
      - name: Send Slack notification
        run: |
          curl -X POST ${{ secrets.SLACK_WEBHOOK }} \
            -H 'Content-Type: application/json' \
            -d '{
              "text": "AIOX Course Platform - Deployment Status",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*AIOX Course Platform*\n*Branch:* ${{ github.ref_name }}\n*Status:* Success\n*Commit:* ${{ github.sha }}\n*Author:* ${{ github.actor }}"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "View on GitHub"
                      },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    },
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "Visit Application"
                      },
                      "url": "https://curso-aiox.vercel.app"
                    }
                  ]
                }
              ]
            }'

